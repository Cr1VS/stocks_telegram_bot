# Указываем, какие модули будут импортироваться при использовании from bot import *
# Это помогает контролировать публичный интерфейс пакета

try:
    __all__ = ["handlers", "messages", "config", "logic"]  # Добавьте сюда другие модули, если появятся
except Exception as e:
    # Логируем ошибку, если вдруг список __all__ вызывает исключение
    print(f"⚠️ Ошибка при инициализации пакета bot: {e}")
